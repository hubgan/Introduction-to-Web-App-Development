import { PurchaseStatusFilterPipe } from './purchase-status-filter.pipe';

describe('PurchaseStatusFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PurchaseStatusFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
