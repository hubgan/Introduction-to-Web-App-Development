import { TransformMoneyPipe } from './transform-money.pipe';

describe('TransformMoneyPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformMoneyPipe();
    expect(pipe).toBeTruthy();
  });
});
